
# Run lint-staged to format staged files with Prettier
npx --no-install lint-staged

# Optionally bump package patch version and re-add changes (keeps changelog minimal)
npm --no-git-tag-version version patch && git add .
